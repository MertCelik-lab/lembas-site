backend:
  name: github
  repo: kullanici_adi/depo_adi # Kendi GitHub kullanıcı adını ve depo (repository) adını yaz
  branch: main # Kullandığın branch hangisiyse (main veya master)

media_folder: "public/images" # Görsellerin yükleneceği klasör yolu
public_folder: "/images"

collections: # Admin panelinde düzenlemek istediğin içerik türleri (Örn: Blog)
  - name: "blog"
    label: "Blog"
    folder: "content/blog" # Yazıların kaydedileceği klasör
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Başlık", name: "title", widget: "string" }
      - { label: "Yayın Tarihi", name: "date", widget: "datetime" }
      - { label: "İçerik", name: "body", widget: "markdown" }